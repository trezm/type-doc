import { stub } from 'sinon';
import { expect } from 'chai';
import { typeChecker } from '../typeChecker';

describe('classes', () => {
  it.skip('should have be able to test against class methods', () => {

  });

  it.skip('should respect `this.`', () => {

  });
});
